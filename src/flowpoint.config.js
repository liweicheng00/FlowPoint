const SVG_INITIAL_HEIGHT = 1000
const SVG_INITIAL_WIDTH = 1000
const EMPTY_STATE = 'emptyState';

// use mixin to import to the project
export default {
    SVG_INITIAL_HEIGHT,
    SVG_INITIAL_WIDTH,
    EMPTY_STATE
}